import React from 'react';
import ArticleList from '../../components/ArticleList';
import './home.css';

const Home = () => {
	return (
		<section className="container">
			<ArticleList />
		</section>
	);
};

export default Home;
