import React from 'react';
import ArticleList from '../../components/ArticleList';

const Home = () => {
	return (
		<section className="container">
			<ArticleList />
		</section>
	);
};

export default Home;
